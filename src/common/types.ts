export type ISliderSettings = {
    arrows?: boolean,
    autoplay?: boolean,
    dots?: boolean,
    infinite?: boolean,
    speed?: number,
    autoplaySpeed?: number,
    slidesToShow?: number,
    slidesToScroll?: number,  
}